using System;
namespace classes
{
    class Program
    {
        static void Main(string[] args)
        {
            double a, b, c = Convert.ToDouble(Console.ReadLine());
            double delta, x1, x2 = 0;
            delta = b * b - 4 * a * c;
            x1 = (-b + Math.Sqrt(delta)) / 2 * a;
        }
    }
    class carro
    {
        private String _cor;
        private String tamanho_roda;
        double a, b, c = 0;
       
        private void motor()
        {
            Console.WriteLine();
        }
    }
}
